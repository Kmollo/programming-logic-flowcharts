// DEBUG Exercise - 03 - Kaleb Whybrew
//
// Errors found and corrected:
//  1. String constant missing quotes: EOFNAME = ZZZZ -> EOFNAME = "ZZZZ".
//  2. Broken mainline logic. The original wrapped housekeeping(), mainLoop(),
//     and finish() each in its own while loop. Correct structure is:
//        housekeeping()          // runs once
//        while name not equal to EOFNAME
//           mainLoop()           // repeats until the sentinel is entered
//        endwhile
//        finish()                // runs once
//  3. housekeeping() prompted for the first name but never read it. Added
//     "input name" (the priming read) so the while condition has a value.
//  4. "gross" was used but never declared. Added "num gross" to mainLoop().
// ---------------------------------------------------------------

// This pseudocode is intended to display
// employee net pay values. All employees have a standard
// $45 deduction from their checks.
// If an employee does not earn enough to cover the deduction
// an error message is displayed.
// This example is modularized.
start
   Declarations
      string name
      string EOFNAME = "ZZZZ"
   housekeeping()
   while name not equal to EOFNAME
      mainLoop()
   endwhile
   finish()
stop

housekeeping()
   output "Enter first name or ", EOFNAME, " to quit "
   input name
return

mainLoop()
   Declarations
      num hours
      num rate
      num DEDUCTION = 45
      num gross
      num net
   output "Enter hours worked for ", name
   input hours
   output "Enter hourly rate for ", name
   input rate
   gross = hours * rate
   net = gross - DEDUCTION
   if net > 0 then
      output "Net pay for ", name, " is ", net
   else
      output "Deductions not covered. Net is 0."
   endif
   output "Enter next name or ", EOFNAME, " to quit "
   input name
return

finish()
   output "End of job"
return
